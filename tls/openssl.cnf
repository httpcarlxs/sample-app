[ req ]
default_bits       = 2048
prompt             = no
default_md         = sha256
distinguished_name = dn
req_extensions     = req_ext

[ dn ]
C = BR
ST = Paraiba
L = Campina Grande
O = Sample App
CN = flask-server

[ req_ext ]
subjectAltName = @alt_names

[ alt_names ]
DNS.1 = flask-server
